
# First generate a Request Token


ReqToken <- getReqToken()

# Parse the output

userToken <- ReqToken[2,2]
userSecret <- ReqToken[3,2]

# Then generate an authorisation link

authURL <- authToken(userToken)

5780000

AccessToken <- getAccToken(5780000,userToken, userSecret)
